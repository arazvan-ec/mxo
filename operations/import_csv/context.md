# Import CSV — Context

## Current State
- Initial prompt defined
- No real executions yet

## Refinement Ideas
- Support multiple CSV formats (auto-detect column mapping)
- Allow column name aliases (e.g., "nombre" → "recipient_name")
- Batch-create routes from imported shipments (combine with auto_assign_routes)
